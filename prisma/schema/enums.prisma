enum UserRole {
  CANDIDATE
  RECRUITER
  CREATOR
  EVALUATOR
  ADMIN
}

enum ProblemType {
  CODING
  MCQ
  WRITTEN
}

enum Difficulty {
  EASY
  MEDIUM
  HARD
}

enum AssessmentStatus {
  DRAFT
  PUBLISHED
  ACTIVE
  CLOSED
}

enum InvitationStatus {
  INVITED
  ACCEPTED
  DECLINED
  EXPIRED
}

enum AttemptStatus {
  NOT_STARTED
  IN_PROGRESS
  SUBMITTED
  EXPIRED
}

enum SubmissionStatus {
  PENDING
  ACCEPTED
  WRONG_ANSWER
  EVALUATED
}

enum EvaluationType {
  AUTOMATIC
  MANUAL
}